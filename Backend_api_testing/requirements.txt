# Project Tensei — Gateway (Backend_api_testing) dependencies
fastapi>=0.110          # web framework: REST endpoints + CORS
uvicorn[standard]>=0.29 # ASGI server to run the gateway
boto3>=1.34             # AWS SDK — talks to AgentCore harnesses
pydantic>=2.0           # request/response validation (installed with fastapi)
